说明：
nginx 相关的配置 不是必须的，这里提供的只是和nginx 搭配使用时的一种配置方式而已
目前支持两种方式， 一种是域名代理，一种是端口代理
域名代理的意思就是说 访问不同的域名 转发到不同的具体服务上
端口代理的意思就是说 通过不同的端口进行访问时，转发到不同的服务上

很显然，端口代理的话，需要监听很多的端口，而域名代理的话则不需要，只需要监听一个地址，
然后对不同的域名执行转发即可。

域名代理 需要指定类型为http
端口代理 需要指定类型为tcp 或http



